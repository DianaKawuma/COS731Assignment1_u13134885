export class Clothing {
  constructor(public id: number, public itemName: string, public price: number,
              public photolink: string, public category: string, public categoryType: string) {
  }
}
